import Link from "next/link";import {ArrowLeft} from "lucide-react";import {notFound} from "next/navigation";import {requireUser} from "@/lib/auth";import {OrganisationAdminForm} from "@/components/record-admin-forms";
// @ts-expect-error Supabase RPC rows are unknown without generated database types.
export default async function OrganisationSettings({params}:{params:Promise<{organisationId:string}>}){const {organisationId}=await params;const {supabase}=await requireUser();const {data:org}=await supabase.rpc("get_admin_organisation",{target_organisation:organisationId}).maybeSingle();if(!org)notFound();return <div className="mx-auto max-w-3xl"><Link href={`/app/${organisationId}`} className="inline-flex items-center gap-2 text-sm font-semibold text-[#0c5b45]"><ArrowLeft size={16}/> Organisation</Link><h1 className="my-6 text-3xl font-semibold">Organisation administration</h1><OrganisationAdminForm record={org}/></div>}
