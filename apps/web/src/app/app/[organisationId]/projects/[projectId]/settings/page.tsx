import Link from "next/link";import {ArrowLeft} from "lucide-react";import {notFound} from "next/navigation";import {requireUser} from "@/lib/auth";import {ProjectAdminForm} from "@/components/record-admin-forms";
// @ts-expect-error Supabase RPC rows are unknown without generated database types.
export default async function ProjectSettings({params}:{params:Promise<{organisationId:string;projectId:string}>}){const {organisationId,projectId}=await params;const {supabase}=await requireUser();const {data:project}=await supabase.rpc("get_manageable_project",{target_organisation:organisationId,target_project:projectId}).maybeSingle();if(!project)notFound();return <div className="mx-auto max-w-3xl"><Link href={`/app/${organisationId}/projects/${projectId}/documents`} className="inline-flex items-center gap-2 text-sm font-semibold text-[#0c5b45]"><ArrowLeft size={16}/> Project MDR</Link><h1 className="my-6 text-3xl font-semibold">Project administration</h1><ProjectAdminForm record={project}/></div>}
