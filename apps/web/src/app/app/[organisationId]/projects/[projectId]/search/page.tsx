import Link from "next/link";import { ArrowLeft,ShieldCheck } from "lucide-react";import { requireProject } from "@/lib/auth";import { ProjectSearch } from "@/components/project-search";
export default async function SearchPage({params}:{params:Promise<{organisationId:string;projectId:string}>}){const {organisationId,projectId}=await params;await requireProject(organisationId,projectId);const basePath=`/app/${organisationId}/projects/${projectId}`;return <div className="mx-auto max-w-6xl"><Link href={`${basePath}/documents`} className="inline-flex items-center gap-2 text-sm font-semibold text-[#0c5b45]"><ArrowLeft size={16}/> Back to document register</Link><div className="mt-6"><p className="text-xs font-bold uppercase tracking-[.16em] text-[#e8733f]">Engineering discovery</p><h1 className="mt-2 text-3xl font-semibold tracking-[-.04em]">Search project evidence</h1><p className="mt-2 flex items-center gap-2 text-sm text-[#617083]"><ShieldCheck size={15}/> Full-text and semantic retrieval remain inside this authorised project.</p></div><div className="mt-8"><ProjectSearch endpoint={`/api/v1/organisations/${organisationId}/projects/${projectId}/search`} basePath={basePath}/></div></div>}
