import Link from "next/link"; import { ArrowUpRight, Building2, Plus } from "lucide-react"; import { requireUser } from "@/lib/auth"; import { createOrganisation } from "./actions";
export default async function AppHome(){const {supabase}=await requireUser();const {data:orgs}=await supabase.from("organisation_access").select("organisation_id,name,slug,role").order("name");return <div className="mx-auto max-w-6xl"><div className="flex flex-wrap items-end justify-between gap-4"><div><p className="text-xs font-bold uppercase tracking-[.16em] text-[#0c5b45]">Workspace</p><h1 className="mt-2 text-3xl font-semibold tracking-[-.04em]">Your organisations</h1><p className="mt-2 text-[#65736f]">Choose a secure tenant or create your first workspace.</p></div></div><div className="mt-8 grid gap-5 lg:grid-cols-[1fr_360px]"><section className="grid content-start gap-3">{orgs?.length?orgs.map(o=><Link href={`/app/${o.organisation_id}`} key={o.organisation_id} className="ev-card flex items-center gap-4 p-5 transition hover:-translate-y-0.5"><span className="grid size-12 place-items-center rounded-xl bg-[#e8f1ed] text-[#0c5b45]"><Building2/></span><div className="flex-1"><h2 className="font-semibold">{o.name}</h2><p className="mt-1 text-xs uppercase tracking-[.1em] text-[#65736f]">{String(o.role).replaceAll("_"," ")}</p></div><ArrowUpRight size={18}/></Link>):<div className="ev-card p-10 text-center text-[#65736f]">No organisation memberships yet.</div>}</section><form action={createOrganisation} className="ev-card h-fit p-6"><div className="flex items-center gap-2"><Plus size={18} className="text-[#0c5b45]"/><h2 className="font-semibold">Create organisation</h2></div><label className="mt-6 block"><span className="ev-label">Organisation name</span><input className="ev-input" name="name" required/></label><label className="mt-4 block"><span className="ev-label">URL slug</span><input className="ev-input" name="slug" pattern="[a-z0-9]+(?:-[a-z0-9]+)*" placeholder="north-field-engineering" required/></label><button className="ev-button mt-5 w-full">Create secure workspace</button></form></div></div>}
